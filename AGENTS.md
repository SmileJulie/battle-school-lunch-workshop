# AGENTS.md

이 문서는 AI 코딩 에이전트가 이 저장소에서 작업할 때 따라야 할 프로젝트 규칙이다.
사람을 위한 개요와 기여 절차는 `README.md`와 `CONTRIBUTING.md`를 우선 참고한다.

> [!NOTE]
> 아래 저장소 구조와 명령 중 일부는 프론트엔드·백엔드 애플리케이션이 구현되기
> 전을 기준으로 작성되었다. 앱 구현이 진행되면 실제 디렉터리, 파일, 스크립트와
> 도구 이름에 맞춰 이 문서 전체(구조, 명령, 기술 스택 표)를 반드시 함께 갱신한다.

## 프로젝트 개요

급식배틀은 NEIS 공개 API를 이용해 학교를 검색하고, 선택한 날짜 범위의 중식
정보를 보여 주는 웹 애플리케이션이다.

- 프론트엔드는 React와 TypeScript로 사용자 인터페이스를 제공한다.
- 백엔드는 Python으로 프론트엔드 API와 NEIS 연동을 제공한다.
- 프론트엔드는 NEIS API를 직접 호출하지 않고 백엔드 API만 호출한다.
- NEIS 요청과 응답의 계약 원본은 `data/openapi.json`이다.
- 기본 사용자 흐름은 학교 검색, 학교 선택, 날짜 범위 선택, 급식 결과 표시 순서다.
- 급식 조회는 중식(`MMEAL_SC_CODE=2`)을 기준으로 한다.

## 저장소 구조

```text
/
├── .github/                 이슈/PR 템플릿, CODEOWNERS, Dependabot, CI
├── backend/                 Python 백엔드 애플리케이션
│   ├── pyproject.toml       Python 패키지 및 개발 도구 설정
│   ├── tests/
│   │   ├── unit/            외부 I/O 없는 단위 테스트
│   │   └── integration/     API 및 NEIS 클라이언트 통합 테스트
│   └── Dockerfile           백엔드 컨테이너 정의
├── data/
│   ├── openapi.json         NEIS API 계약의 단일 원본
│   └── *.xlsx               원본 NEIS 명세 자료
├── e2e/                     브라우저 E2E 테스트
├── frontend/                React + TypeScript 프론트엔드
│   ├── src/                 컴포넌트, 페이지, API 클라이언트
│   ├── tests/               프론트엔드 통합 테스트
│   ├── package.json         npm 스크립트와 의존성
│   └── Dockerfile           프론트엔드 컨테이너 정의
└── compose.yaml             프론트엔드와 백엔드 오케스트레이션
```

구현 과정에서 이름이 달라진 경로가 있다면 실제 디렉터리와 매니페스트를 기준으로
이 구조와 아래 명령을 함께 갱신한다. 존재하지 않는 경로나 도구를 문서에만 먼저
추가하지 않는다.

## 기술 스택과 도구

| 영역 | 기술 및 도구 |
| --- | --- |
| 프론트엔드 | React, TypeScript, Node.js 24, npm |
| 프론트엔드 품질 | 프로젝트 `package.json`에 등록된 포맷터, ESLint, 타입 검사, 빌드 도구 |
| 프론트엔드 테스트 | 프로젝트에 설정된 통합 테스트 러너 |
| 백엔드 | Python 3.12, `pyproject.toml`, pip editable install |
| 백엔드 품질 | `pyproject.toml`에 설정된 포맷터, 린터, 타입 검사기 |
| 백엔드 테스트 | pytest |
| E2E | 프로젝트에 설정된 브라우저 E2E 테스트 러너 |
| 컨테이너 | Docker, Docker Compose |

락 파일은 재현 가능한 설치의 기준이다. 프론트엔드는 `package-lock.json`, 백엔드는
`pyproject.toml`과 프로젝트에서 사용하는 락 파일을 변경한 의존성과 함께 갱신한다.

## 작업 원칙

- 요청 범위에 필요한 최소 변경만 수행하고 기존 패턴을 우선 재사용한다.
- `README.md`, `PRD.md`, `TRD.md`, 더 가까운 하위 `AGENTS.md`가 있으면 해당 문서의
  구체적인 규칙을 우선한다.
- 기능과 무관한 리팩터링이나 미래를 위한 추상화를 함께 넣지 않는다.
- 공개 API와 컴포넌트의 기존 동작을 보존하고, 동작이 바뀌면 테스트와 문서를 함께
  수정한다.
- 오류를 삼키거나 성공처럼 보이는 기본값으로 바꾸지 않는다. 사용자 또는 호출자가
  처리할 수 있는 명확한 오류로 전달한다.
- 새 의존성을 추가하기 전에 표준 라이브러리와 기존 의존성으로 해결할 수 있는지
  확인한다.

## 설치와 로컬 개발

명령은 저장소 루트에서 시작하며, 각 매니페스트가 존재하는 경우에 실행한다.

### 환경 변수

- 저장소 루트에 `.env.example`이 있으면 이를 복사해 `.env`를 만들고
  `NEIS_API_KEY`를 실제 값으로 채운다. `.env`는 커밋하지 않는다.
- 자동화 테스트는 NEIS API 키 없이 통과해야 한다. 로컬에서 실제 NEIS 데이터로
  수동 검증할 때만 `.env`의 키를 사용한다.

### 프론트엔드

```sh
cd frontend
npm ci
npm run dev
```

- 의존성을 의도적으로 변경할 때만 `npm install`을 사용한다.
- 실행 가능한 품질 명령은 `npm run`으로 확인하고 `package.json`에 정의된 스크립트만
  사용한다.

### 백엔드

```sh
cd backend
python -m venv .venv
python -m pip install -e ".[dev]"
```

가상 환경을 활성화한 뒤 `pyproject.toml`에 정의된 ASGI 애플리케이션과 개발 서버
명령으로 실행한다. 모듈 경로나 포트를 추측해서 새 명령을 만들지 않는다.

### 전체 애플리케이션

```sh
docker compose config
docker compose up --build
```

종료할 때는 `docker compose down`을 사용한다. Compose 서비스 간 통신에는 서비스
이름을 사용하고, 컨테이너 내부에서 `localhost`로 다른 서비스를 참조하지 않는다.

## 빌드와 품질 검사

변경한 영역에서 매니페스트에 정의된 검사를 모두 실행한다. 현재 CI가 보장하는 기본
명령은 다음과 같다.

```sh
cd frontend
npm ci
npm run build --if-present
npm test --if-present

cd ../backend
python -m pip install -e ".[dev]"
pytest

cd ..
docker compose config
```

포맷팅, 린트와 타입 검사 스크립트가 구성되어 있으면 실행한다.

```sh
cd frontend
npm run format
npm run lint
npm run typecheck
```

백엔드는 `python -m pip show <tool>` 또는 `backend/pyproject.toml`의
`[tool.*]`/`[dependency-groups]` 설정을 확인해 실제로 설치된 포맷터, 린터, 타입
검사기 명령을 그대로 사용한다(예: 프로젝트가 ruff와 mypy를 채택했다면
`uv run ruff format --check .`, `uv run ruff check .`, `uv run mypy .`). 이 문서에
없는 명령을 추측해서 만들지 않는다. 도구가 아직 구성되지 않았다면 임의로 설치하거나
명령을 통과시키기 위한 빈 스크립트를 추가하지 않는다. 도구 도입 자체가 작업
범위라면 설정, 의존성, CI와 이 문서를 함께 갱신한다.

## 코딩 지침

### React와 TypeScript

- `strict` 타입 검사를 유지하고 `any`, 이중 타입 단언, 불필요한 non-null 단언을
  사용하지 않는다.
- 서버 응답은 신뢰하지 말고 API 경계에서 검증하거나 명확한 타입으로 변환한다.
- 네트워크 호출은 공용 API 클라이언트 계층에 모은다. React 컴포넌트에서 NEIS URL을
  사용하거나 NEIS API를 직접 `fetch`하지 않는다.
- 로딩, 빈 결과, 유효성 오류와 서버 오류를 서로 구분해 사용자에게 표시한다.
- 날짜 범위는 요청 전에 검증하며 시작일이 종료일보다 늦은 요청을 보내지 않는다.
- 상태는 가능한 한 사용하는 컴포넌트 가까이에 두고, 파생 상태를 중복 저장하지
  않는다.

### Python

- 공개 함수, 메서드와 데이터 구조에 타입 힌트를 사용하고 타입 검사 오류를 억지
  캐스팅으로 숨기지 않는다.
- API 요청과 응답은 명시적인 스키마로 검증한다.
- NEIS HTTP 호출은 전용 클라이언트 계층에 격리하고 타임아웃과 오류 매핑을
  명시한다.
- 광범위한 `except Exception`으로 오류를 숨기지 않는다. 복구 가능한 예외만
  구체적으로 처리한다.
- 설정과 시크릿은 환경 변수 또는 프로젝트 설정 객체로 주입한다.
- 동기 요청 처리 중 블로킹 I/O를 추가하지 않는다. 프로젝트의 기존 동기 또는
  비동기 경계를 유지한다.

## API 계약

- `data/openapi.json`은 NEIS 학교 및 급식 API의 단일 계약 원본이다.
- NEIS 연동을 변경하기 전에 해당 경로, 파라미터, 필수 여부, 응답과 오류 스키마를
  확인한다.
- `ATPT_OFCDC_SC_CODE`와 `SD_SCHUL_CODE`를 임의 값으로 대체하지 않는다.
- 급식 조회에는 중식 코드 `2`를 사용한다.
- 계약에서 생성되는 클라이언트, 타입 또는 문서는 직접 수정하지 않는다. 원본 명세나
  생성 설정을 수정한 뒤 프로젝트의 생성 명령을 다시 실행한다.
- 프론트엔드와 백엔드 사이의 내부 API 계약을 변경하면 양쪽 구현과 관련 통합/E2E
  테스트를 같은 변경에서 갱신한다.

## 테스트

| 종류 | 위치 | 실행 |
| --- | --- | --- |
| 프론트엔드 통합 | `frontend/tests/` 또는 설정된 `*.test.*` 경로 | `cd frontend && npm test` |
| 백엔드 단위 | `backend/tests/unit/` | `cd backend && pytest tests/unit` |
| 백엔드 통합 | `backend/tests/integration/` | `cd backend && pytest tests/integration` |
| 백엔드 전체 | `backend/tests/` | `cd backend && pytest` |
| E2E | `e2e/` | `e2e/package.json` 또는 프로젝트 설정의 테스트 명령 |

- 프론트엔드는 구현 세부 단위 테스트보다 사용자 행동과 API 연동을 검증하는 통합
  테스트를 작성한다.
- 백엔드 단위 테스트는 외부 I/O 없이 비즈니스 규칙, 날짜 검증과 응답 변환을
  검증한다.
- 백엔드 통합 테스트는 API 라우팅, 스키마와 NEIS 클라이언트 연결을 검증한다.
- E2E 테스트는 학교 검색, 학교 선택, 날짜 범위 선택, 중식 결과 표시의 핵심 흐름과
  오류 및 빈 결과를 검증한다.
- 자동화 테스트에서 실제 NEIS 서비스에 접근하지 않는다. HTTP 경계에서 현실적인
  응답과 오류를 모킹하고 API 키를 요구하지 않게 한다.
- 버그 수정에는 수정 전 실패하고 수정 후 통과하는 회귀 테스트를 추가한다.
- 시간, 네트워크와 실행 순서에 의존하는 불안정한 테스트를 만들지 않는다.

## 보안과 생성 파일

- API 키, 토큰, 비밀번호, 연결 문자열과 실제 `.env` 파일을 커밋하지 않는다.
- 예시 환경 파일에는 변수 이름과 안전한 자리표시자만 넣는다.
- NEIS API 키를 프론트엔드 코드, 브라우저 번들, 로그, 오류 응답 또는 테스트
  픽스처에 포함하지 않는다.
- 사용자 입력과 외부 API 응답을 검증하고, 내부 예외나 시크릿을 클라이언트에
  노출하지 않는다.
- 요청/응답 전체 본문이나 인증 헤더를 로그에 남기지 않는다. 로그에는 요청 식별자,
  경로, 상태 코드 등 디버깅에 필요한 최소 정보만 남긴다.
- 락 파일, 빌드 산출물, 커버리지 결과와 OpenAPI 생성 파일을 손으로 편집하지 않는다.
- 의존성 업데이트는 목적을 분명히 하고 관련 락 파일과 검증 결과를 포함한다.

## 문서화

- 실행 명령, 환경 변수, API 계약 또는 사용자 동작이 바뀌면 관련 `README.md`,
  `PRD.md`, `TRD.md`와 이 문서를 함께 수정한다.
- 문서의 명령은 저장소에서 실제로 실행 가능한 형태로 유지한다.
- 코드만 반복하는 주석은 추가하지 않고, 제약이나 의사결정의 이유가 드러나지 않는
  경우에만 짧게 설명한다.

## Git 커밋과 Pull Request

- 기존 사용자 변경을 덮어쓰거나 관련 없는 파일을 되돌리지 않는다.
- 브랜치는 `feat/`, `fix/`, `docs/` 중 변경 성격에 맞는 접두사를 사용해 생성한다.
- 한 커밋은 하나의 논리적 변경에 집중하고 Conventional Commits 형식(`feat:`,
  `fix:`, `docs:`, `test:`, `refactor:`, `chore:`)을 사용한다.
- PR은 `main`을 대상으로 하며 한 가지 변경 사항에 집중한다.
- PR을 작성할 때는 **반드시** `.github/PULL_REQUEST_TEMPLATE.md`를 활용하고,
  템플릿의 모든 섹션을 삭제하거나 생략하지 않고 유지한다.
- 관련 이슈는 `Closes #<번호>` 형식으로 연결한다.
- 변경 요약과 실제로 실행한 검증 명령을 PR에 기록한다.
- 동작 변경에는 테스트를 추가하고, 관련 포맷팅, 린트, 타입 검사, 테스트, 빌드와
  `docker compose config`가 통과한 뒤 PR을 제출한다.
