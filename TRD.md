# 급식 배틀 - 학교 급식 조회 앱 기술 요구사항

## 1. 문서 목적

이 문서는 `PRD.md`의 제품 요구사항을 구현하기 위한 기술 구조, 컴포넌트 책임,
API 계약, 데이터 흐름, 오류 처리, 실행 환경 및 테스트 전략을 정의한다.

## 2. 기술 원칙

1. 애플리케이션은 React·TypeScript 프론트엔드와 Python 백엔드로 분리한다.
2. 프론트엔드는 백엔드 API만 호출하며 NEIS API를 직접 호출하지 않는다.
3. NEIS 외부 API 계약의 단일 원본은 `data/openapi.json`으로 한다.
4. 프론트엔드와 백엔드 사이의 내부 API 계약은 `src/openapi.json`으로 정의한다.
5. NEIS API 키는 백엔드 런타임 환경 변수로만 주입하고 브라우저에 노출하지 않는다.
6. 프론트엔드와 백엔드는 Docker 이미지로 빌드하고 Docker Compose로 함께 실행한다.

이슈의 “프론트엔드가 `data/openapi.json` 명세를 별도 클라이언트 코드로 구현”한다는
요구는 NEIS 데이터 구조를 이해하고 내부 모델에 반영하라는 의미로 해석한다.
프론트엔드가 생성하거나 직접 구현하는 HTTP 클라이언트의 계약은
`src/openapi.json`이며, `data/openapi.json`을 사용하는 NEIS HTTP 클라이언트는
백엔드에만 존재한다. 이는 프론트엔드의 NEIS 직접 호출 금지와 API 키 비노출
요구사항을 우선하기 위한 명시적 설계 결정이다.

## 3. 전체 아키텍처

```text
사용자 브라우저
    |
    | 내부 HTTP API
    v
React + TypeScript 프론트엔드
    |
    | src/openapi.json 계약
    v
Python 백엔드 API
    |
    | data/openapi.json 계약 + 서버 측 API 키
    v
NEIS 공개 API
```

프론트엔드는 사용자 입력과 화면 상태를 관리하고, 백엔드는 입력 검증, NEIS 요청,
외부 응답 검증 및 내부 응답 변환을 담당한다. 외부 NEIS 응답 구조를 프론트엔드에
그대로 노출하지 않고 앱에 필요한 안정적인 내부 스키마로 변환한다.

## 4. 권장 저장소 구조

```text
/
├── frontend/
│   ├── src/
│   │   ├── api/              # 내부 API 클라이언트
│   │   ├── components/       # 검색, 날짜 범위, 결과 UI
│   │   └── pages/            # 3단계 사용자 흐름
│   ├── tests/                # 프론트엔드 통합 테스트
│   ├── package.json
│   └── Dockerfile
├── backend/
│   ├── src/
│   │   ├── api/              # 내부 API 라우트와 스키마
│   │   ├── clients/          # NEIS 전용 클라이언트
│   │   └── config/           # 환경 기반 설정
│   ├── tests/
│   │   ├── unit/
│   │   └── integration/
│   ├── pyproject.toml
│   └── Dockerfile
├── src/
│   └── openapi.json          # 프론트엔드-백엔드 내부 API 계약
├── data/
│   └── openapi.json          # NEIS 외부 API 계약
├── e2e/
└── compose.yaml
```

실제 프레임워크가 정한 패키지 구조와 생성 명령을 따르되, 외부 API 클라이언트와
내부 API 라우트의 책임은 분리해야 한다.

## 5. 컴포넌트 책임

### 5.1 프론트엔드

- React와 TypeScript `strict` 모드로 UI를 구현한다.
- 학교 검색, 학교 선택, 날짜 범위 선택, 중식 결과의 화면 상태를 관리한다.
- 공백 검색어, 필수 입력 누락, 역전된 날짜 범위를 요청 전에 검증한다.
- `src/openapi.json`에서 정의한 내부 API를 전용 클라이언트 계층으로 호출한다.
- `data/openapi.json`의 NEIS 데이터 의미를 참고해 앱 모델을 표현할 수 있지만,
  NEIS URL 또는 API 키를 사용하거나 NEIS API를 직접 `fetch`하지 않는다.
- 로딩, 빈 결과, 입력 오류, 서버 오류를 서로 다른 UI 상태로 표현한다.
- 최신 요청만 화면에 반영되도록 요청 취소 또는 요청 식별자를 사용한다.

프론트엔드의 핵심 상태는 다음과 같다.

```text
schoolQuery
schoolSearchState: idle | loading | success | empty | error
schoolResults
selectedSchool
dateRange: startDate, endDate
mealQueryState: idle | loading | success | empty | error
mealResults
```

선택된 학교와 급식 결과처럼 다른 상태에서 계산할 수 있는 값은 중복 저장하지
않는다. 학교가 변경되면 급식 결과를 초기화하며 날짜만 변경된 경우에는 사용자가
다시 조회하기 전까지 기존 결과가 현재 조건과 다름을 명확히 처리한다.

### 5.2 백엔드

- Python 기반 HTTP API를 제공하고 요청 및 응답을 명시적 스키마로 검증한다.
- 학교 검색어, 학교 식별 코드 및 날짜 범위를 서버에서 다시 검증한다.
- NEIS HTTP 통신을 전용 클라이언트에 격리한다.
- `data/openapi.json`을 근거로 경로, 쿼리 파라미터, 응답 및 오류를 구현한다.
- NEIS 요청에는 `Type=json`, 유효한 페이지 값과 서버 측 API 키를 사용한다.
- 급식 요청에는 `ATPT_OFCDC_SC_CODE`, `SD_SCHUL_CODE`,
  `MMEAL_SC_CODE=2`, `MLSV_FROM_YMD`, `MLSV_TO_YMD`를 전달한다.
- 외부 응답을 검증하고 앱의 내부 응답 모델로 변환한다.
- 명시적 연결 및 읽기 타임아웃을 적용하고 외부 오류를 내부 오류 체계로 매핑한다.
- 내부 예외, 스택 추적, API 키 또는 NEIS 요청의 민감한 값을 응답과 로그에
  노출하지 않는다.

동기 요청 처리에서 블로킹 HTTP 호출을 추가하지 않는다. 선택한 Python 웹
프레임워크의 비동기 경계를 유지하고 그에 맞는 HTTP 클라이언트를 사용한다.

### 5.3 NEIS 클라이언트

NEIS 클라이언트는 다음 두 작업만 외부 API에 수행한다.

| 작업 | NEIS 경로 | 주요 파라미터 |
| --- | --- | --- |
| 학교 검색 | `GET /schoolInfo` | `SCHUL_NM`, `Type=json`, `pIndex=1`, `pSize=20` |
| 중식 조회 | `GET /mealServiceDietInfo` | 교육청 코드, 학교 코드, `MMEAL_SC_CODE=2`, 시작일, 종료일, 페이지 값 |

학교 검색은 첫 페이지에서 최대 20개를 반환한다. NEIS의 `list_total_count`가
20보다 크면 내부 응답의 `hasMore`를 `true`로 설정하고 추가 페이지를 자동 조회하지
않는다. 프론트엔드는 검색어를 구체화하도록 안내한다.

중식 조회는 `pSize=100`으로 시작한다. 최대 조회 기간이 31일이고 중식은 날짜당
한 건이므로 정상 응답은 한 페이지에 수용된다. 그럼에도 `list_total_count`가
현재까지 수집한 행 수보다 크면 `pIndex`를 증가시켜 모든 페이지를 수집하며,
페이지별 중복 행은 학교 코드, 급식일, 식사 코드 조합으로 제거한다.

NEIS 응답의 `schoolInfo` 또는 `mealServiceDietInfo` 배열과 최상위 `RESULT` 형식을
모두 처리해야 한다. 결과 없음 코드는 빈 목록으로 변환할 수 있지만 인증, 입력,
트래픽, 서버 및 데이터베이스 오류는 성공 또는 빈 결과로 변환하지 않는다.

## 6. 내부 API 계약

`src/openapi.json`은 구현 시 생성하며 최소한 아래 세 엔드포인트와 스키마를
OpenAPI 형식으로 정의한다. 프론트엔드 클라이언트와 백엔드 라우트는 이 계약을
동시에 만족해야 한다.

### 6.1 학교 검색

```http
GET /api/schools?query={부분 학교명}
```

요구사항:

- `query`: 앞뒤 공백을 제거한 비어 있지 않은 문자열
- 성공: `200 OK`
- 검색 결과 없음: `200 OK`와 빈 `schools` 배열
- 잘못된 입력: `400 Bad Request`

응답 예시:

```json
{
  "schools": [
    {
      "officeCode": "B10",
      "schoolCode": "7010057",
      "name": "예시고등학교",
      "officeName": "서울특별시교육청",
      "regionName": "서울특별시",
      "schoolType": "고등학교"
    }
  ],
  "hasMore": false
}
```

`officeCode`와 `schoolCode`의 조합을 학교 식별자로 사용한다. 동명 학교를
구분하기 위해 지역, 교육청 또는 학교 종류를 함께 반환한다. `hasMore`가 `true`이면
일치 결과가 20개를 초과했음을 뜻하며, 클라이언트는 별도 페이지를 요청하는 대신
더 구체적인 검색어 입력을 안내한다.

### 6.2 중식 조회

```http
GET /api/meals?officeCode={교육청 코드}&schoolCode={학교 코드}&from={YYYY-MM-DD}&to={YYYY-MM-DD}
```

요구사항:

- 모든 쿼리 파라미터는 필수다.
- 날짜는 ISO 달력 날짜로 받고 백엔드에서 NEIS의 `YYYYMMDD` 형식으로 변환한다.
- `from`은 `to`보다 늦을 수 없다.
- `from`과 `to`를 모두 포함한 기간은 최대 31일이다.
- 성공: `200 OK`
- 급식 정보 없음: `200 OK`와 빈 `meals` 배열
- 잘못된 입력, 역전된 날짜 또는 31일을 초과한 범위: `400 Bad Request`

응답 예시:

```json
{
  "school": {
    "officeCode": "B10",
    "schoolCode": "7010057"
  },
  "from": "2026-08-14",
  "to": "2026-08-14",
  "meals": [
    {
      "date": "2026-08-14",
      "mealType": "중식",
      "dishes": ["쌀밥", "미역국", "배추김치"],
      "calories": "650.0 Kcal",
      "nutrition": "탄수화물(g): 90.0\n단백질(g): 25.0",
      "origin": "쌀: 국내산",
      "servings": 500
    }
  ]
}
```

`dishes`는 NEIS `DDISH_NM`의 항목 경계를 보존해 배열로 변환한다. `calories`,
`nutrition`, `origin`, `servings`는 외부 데이터에 없을 수 있으므로 내부 계약에서
nullable 또는 optional 여부를 명시한다. 문자열에 포함된 HTML 구분자는 백엔드
변환 단계에서 줄바꿈 등 안전한 텍스트 구조로 바꾸며 프론트엔드는 이를 임의
HTML로 삽입하지 않는다.

### 6.3 공통 오류

내부 API 오류는 일관된 형식으로 반환한다.

```json
{
  "error": {
    "code": "UPSTREAM_UNAVAILABLE",
    "message": "급식 정보를 불러오지 못했습니다.",
    "requestId": "요청 추적 식별자"
  }
}
```

| HTTP 상태 | 내부 코드 예 | 의미 |
| --- | --- | --- |
| `400` | `INVALID_REQUEST`, `INVALID_DATE_RANGE` | 입력 또는 날짜 범위 오류 |
| `502` | `UPSTREAM_ERROR` | NEIS가 오류 응답 또는 유효하지 않은 응답 반환 |
| `503` | `UPSTREAM_UNAVAILABLE` | NEIS 연결 실패 또는 타임아웃 |
| `500` | `INTERNAL_ERROR` | 예상하지 못한 백엔드 오류 |

사용자 메시지는 안전하고 이해 가능한 문구로 제공하며, 상세 원인은 요청 식별자와
함께 서버 로그에서 진단한다.

### 6.4 헬스 체크

```http
GET /health
```

- 정상 실행 중이면 `200 OK`와 `{"status": "ok"}`를 반환한다.
- API 키, 설정값 또는 내부 진단 정보를 응답에 포함하지 않는다.
- 프로세스의 요청 처리 가능 여부만 확인하는 가벼운 엔드포인트로 구현하며 NEIS를
  호출하지 않는다.
- Docker Compose의 백엔드 헬스 체크는 이 엔드포인트를 사용한다.

## 7. 데이터 흐름

### 7.1 학교 검색

1. 프론트엔드가 검색어를 정규화하고 빈 값 여부를 검증한다.
2. 프론트엔드 내부 API 클라이언트가 `/api/schools`를 호출한다.
3. 백엔드가 입력을 검증하고 NEIS 클라이언트에 검색을 위임한다.
4. NEIS 클라이언트가 `/schoolInfo`를 호출하고 응답 계약을 검증한다.
5. 백엔드가 필요한 학교 필드만 내부 모델로 변환한다.
6. 프론트엔드가 결과, 빈 상태 또는 오류 상태를 표시한다.

### 7.2 중식 조회

1. 프론트엔드가 학교 선택과 날짜 범위의 유효성을 확인한다.
2. 프론트엔드가 `/api/meals`에 학교 코드와 날짜 범위를 전달한다.
3. 백엔드가 입력을 검증하고 날짜를 NEIS 형식으로 변환한다.
4. NEIS 클라이언트가 중식 코드 `2`를 포함해 `/mealServiceDietInfo`를 호출한다.
5. 백엔드가 응답을 검증하고 날짜 오름차순의 내부 급식 모델로 변환한다.
6. 프론트엔드가 날짜별 결과 또는 명시적인 빈 상태를 표시한다.

## 8. 입력 및 응답 검증

- 프론트엔드와 백엔드가 각각 입력을 검증하며 서버 검증을 최종 기준으로 한다.
- 학교 검색어는 공백 제거 후 비어 있지 않아야 하며 길이 제한을 내부 계약에
  명시한다.
- 날짜는 실제 존재하는 ISO 날짜여야 하고 시작일은 종료일보다 늦을 수 없다.
- 조회 기간은 시작일과 종료일을 포함해 최대 31일로 제한하고 초과 시 `400` 오류로
  안내한다.
- 교육청 코드와 학교 코드는 허용된 문자와 길이를 검증한다.
- NEIS 응답은 `data/openapi.json` 스키마에 따라 경계에서 검증한다.
- 필수 식별자나 급식일이 누락된 행은 전체 성공으로 가장하지 않고 외부 응답 오류로
  처리한다.

## 9. 설정 및 보안

백엔드는 최소한 다음 설정을 환경 변수로 받는다.

| 변수 | 용도 | 노출 범위 |
| --- | --- | --- |
| `NEIS_API_KEY` | NEIS API 인증 | 백엔드 컨테이너만 |
| `NEIS_BASE_URL` | NEIS 기본 URL | 백엔드 컨테이너만 |
| `NEIS_TIMEOUT_SECONDS` | 외부 요청 타임아웃 | 백엔드 컨테이너만 |
| `BACKEND_ALLOWED_ORIGINS` | 허용 프론트엔드 Origin | 백엔드 컨테이너만 |

- 실제 값이 든 `.env` 파일은 버전 관리하지 않는다.
- 예시 설정에는 안전한 자리표시자만 사용한다.
- CORS는 배포된 프론트엔드 Origin으로 제한한다.
- 모든 사용자 입력은 쿼리 파라미터로 안전하게 인코딩하며 로그에 민감 정보를
  기록하지 않는다.
- 브라우저 번들 또는 프론트엔드 환경 변수에 `NEIS_API_KEY`를 넣지 않는다.

## 10. Docker Compose 구성

`compose.yaml`은 최소 `frontend`와 `backend` 서비스를 정의한다.

### frontend 서비스

- 프론트엔드 `Dockerfile`로 재현 가능하게 빌드한다.
- 브라우저가 접근할 포트를 공개한다.
- 런타임 또는 빌드 설정에는 내부 백엔드 API의 공개 경로만 포함한다.
- NEIS URL과 API 키를 포함하지 않는다.

### backend 서비스

- 백엔드 `Dockerfile`로 재현 가능하게 빌드한다.
- Compose 네트워크에서 `backend` 서비스 이름으로 접근 가능해야 한다.
- API 키와 외부 URL은 환경 변수로 주입한다.
- 서비스 준비 상태를 판단할 수 있는 헬스 체크 엔드포인트를 제공한다.

프론트엔드 컨테이너 또는 개발 프록시는 Compose 내부 통신 시 `localhost`가 아닌
`backend` 서비스 이름을 사용한다. 시작 순서가 필요하면 단순 프로세스 시작이
아니라 백엔드 헬스 체크를 기준으로 의존성을 정의한다. `docker compose up --build`
한 번으로 두 서비스를 빌드하고 실행할 수 있어야 하며 `docker compose down`으로
종료할 수 있어야 한다.

## 11. 관측성과 오류 처리

- 각 백엔드 요청에 요청 식별자를 부여하거나 전달받아 응답과 구조화 로그에
  포함한다.
- 요청 경로, 상태 코드, 처리 시간과 외부 호출 결과를 기록하되 API 키와 전체
  민감 쿼리는 기록하지 않는다.
- 외부 연결 실패, 타임아웃, NEIS 오류 코드, 응답 검증 실패를 구분한다.
- 광범위한 예외 처리로 오류를 빈 배열 또는 성공 응답으로 바꾸지 않는다.
- 재시도는 멱등적인 조회에 한해 제한된 횟수와 지수 백오프로 적용할 수 있으며,
  입력·인증 오류에는 적용하지 않는다.

## 12. 테스트 전략

실제 NEIS 서비스와 API 키에 의존하지 않는다. HTTP 경계에서
`data/openapi.json`과 일치하는 성공, 빈 결과 및 오류 응답을 모킹한다.

### 12.1 프론트엔드 통합 테스트

구현 세부 단위 테스트 대신 사용자 행동과 내부 API 연동을 검증한다.

- 부분 학교명을 입력하고 결과를 선택하는 흐름
- 검색 로딩, 결과 없음 및 서버 오류 표시
- 학교 미선택 또는 날짜 누락 시 조회 비활성화
- 시작일이 종료일보다 늦을 때 요청을 보내지 않고 오류 표시
- 날짜 범위가 31일을 초과할 때 요청을 보내지 않고 오류 표시
- 학교 검색 결과가 20개를 초과할 때 검색어 구체화 안내
- 유효한 조건으로 조회한 뒤 날짜별 중식 표시
- 급식 정보 없음과 서버 오류의 구분
- 학교 변경 시 이전 급식 결과 초기화
- 경합하는 검색 요청에서 최신 응답만 표시
- 키보드 조작, 연결된 레이블 및 상태 안내 확인

### 12.2 백엔드 단위 테스트

외부 I/O 없이 다음 로직을 검증한다.

- 학교 검색어와 식별 코드 검증
- ISO 날짜 파싱, NEIS 날짜 변환 및 날짜 범위 검증
- 중식 코드 `2` 적용
- NEIS 학교 응답에서 내부 학교 모델로 변환
- NEIS 급식 응답에서 메뉴 배열과 선택 필드로 변환
- 결과 없음 코드와 실제 오류 코드의 구분
- 날짜 정렬 및 누락 필수 필드 처리

### 12.3 백엔드 통합 테스트

백엔드 API와 모킹된 NEIS HTTP 경계를 함께 검증한다.

- `/api/schools` 요청·응답 스키마와 NEIS 파라미터
- `/api/meals` 요청·응답 스키마, 날짜 변환 및 `MMEAL_SC_CODE=2`
- 학교 및 급식 빈 결과의 `200` 응답
- 학교 결과가 20개를 초과할 때 첫 20개와 `hasMore=true` 반환
- 잘못된 입력의 `400` 응답과 외부 호출 미발생
- 31일을 초과한 날짜 범위의 `400` 응답과 외부 호출 미발생
- NEIS 급식 응답이 여러 페이지일 때 전체 수집, 중복 제거 및 날짜 정렬
- NEIS 인증·입력·트래픽·서버 오류의 내부 오류 매핑
- 연결 실패, 타임아웃 및 잘못된 외부 응답 처리
- 오류 응답과 로그에 API 키 또는 내부 예외가 노출되지 않음
- `/health`가 외부 호출 없이 최소 상태만 반환

### 12.4 E2E 테스트

브라우저에서 프론트엔드와 백엔드를 함께 실행하고 전체 사용자 흐름을 검증한다.

1. 학교명 일부를 입력한다.
2. 검색 결과에서 학교를 선택한다.
3. 시작일과 종료일을 선택한다.
4. 조회를 실행한다.
5. 날짜별 중식 메뉴를 확인한다.

별도 시나리오로 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음, 백엔드 오류를
검증한다. E2E 환경에서도 NEIS 호출은 통제된 스텁 또는 모킹 서버로 대체해 테스트가
네트워크와 실제 데이터 변화에 의존하지 않게 한다.

## 13. 기술 인수 조건

- React 프론트엔드는 내부 백엔드 API만 호출하며 NEIS 호출 코드나 API 키를
  포함하지 않는다.
- Python 백엔드는 `data/openapi.json`에 맞는 전용 NEIS 클라이언트를 통해 학교와
  중식 정보를 조회한다.
- `src/openapi.json`에 내부 엔드포인트, 성공·빈 결과·오류 스키마가 정의되어
  프론트엔드와 백엔드의 공통 계약으로 사용된다.
- 학교 검색은 최대 20개와 `hasMore`를 반환하고, 급식 조회 기간은 최대 31일로
  제한된다.
- NEIS 급식 결과가 여러 페이지이면 모든 페이지를 수집해 중복 제거 후 반환한다.
- 중식 요청에는 항상 `MMEAL_SC_CODE=2`가 적용된다.
- Docker Compose로 프론트엔드와 백엔드를 빌드·실행하고 서비스 이름으로 통신하며
  `/health`를 백엔드 준비 상태 확인에 사용한다.
- 프론트엔드 통합 테스트, 백엔드 단위·통합 테스트 및 핵심 흐름 E2E 테스트가
  자동화되어 통과한다.
- 검색 결과 없음, 잘못된 날짜 범위, 급식 정보 없음과 외부 서비스 오류가 서로
  구분된다.
