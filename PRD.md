# 급식 배틀 - 학교 급식 조회 앱

## 1. 문서 목적

이 문서는 사용자가 학교와 날짜 범위를 선택해 날짜별 중식 정보를 조회하는
웹 애플리케이션의 제품 요구사항을 정의한다. 제품 범위와 사용자 경험에 대한 단일
기준으로 사용하며, 구체적인 기술 설계는 `TRD.md`에서 정의한다.

## 2. 개발 목적

NEIS 공개 API의 학교 및 급식 정보를 사용자가 쉽게 검색하고 확인할 수 있는
웹 인터페이스를 제공한다. 사용자는 교육청 코드나 학교 코드를 알지 못해도 학교
이름 일부만으로 학교를 찾고, 원하는 기간의 중식 메뉴를 날짜별로 확인할 수 있어야
한다.

## 3. 대상 사용자

- 특정 초·중·고등학교의 급식 메뉴를 확인하려는 학생과 보호자
- 학교별 급식 정보를 빠르게 조회하려는 교직원 및 일반 사용자
- NEIS의 코드 체계나 공개 API 사용법을 알지 못하는 사용자

## 4. 제품 목표

1. 학교명 일부를 입력해 원하는 학교를 쉽게 찾고 정확한 학교를 선택할 수 있다.
2. 유효한 날짜 범위를 선택해 해당 기간의 중식 정보를 한 번에 조회할 수 있다.
3. 조회 결과와 빈 결과, 입력 오류, 시스템 오류를 명확히 구분해 보여 준다.
4. NEIS API 키와 외부 API의 복잡성을 브라우저 사용자에게 노출하지 않는다.

## 5. 범위

### 5.1 포함 범위

- 부분 학교명 기반 학교 검색
- 검색 결과에서 학교 식별에 필요한 정보 표시 및 단일 학교 선택
- 시작일과 종료일을 포함하는 날짜 범위 선택
- 한 번에 최대 31일의 날짜 범위 조회
- 선택한 학교와 기간에 대한 중식 조회
- 날짜순 급식 결과 표시
- 로딩, 빈 결과, 유효성 오류 및 서버 오류 상태 표시
- 데스크톱과 모바일 웹 브라우저에서 사용할 수 있는 반응형 UI
- AI 에이전트가 학교 후보와 중식 정보를 조회할 수 있는 MCP 도구 제공

### 5.2 제외 범위

- 회원 가입, 로그인 및 사용자별 설정 저장
- 학교 즐겨찾기와 최근 검색 기록의 영구 저장
- 조식·석식 조회
- 학교 간 급식 비교, 점수화 또는 AI 분석
- 메뉴 수정, 급식 신청 및 NEIS 데이터에 대한 쓰기 작업
- 푸시 알림 및 급식 일정 구독
- MCP 도구를 통한 메뉴 수정 또는 NEIS 데이터 쓰기 작업

## 6. 핵심 사용자 흐름

### 1단계: 학교 검색 및 선택

1. 사용자가 학교 이름의 일부를 입력한다.
2. 시스템이 입력값과 일치하는 학교 목록을 표시한다.
3. 사용자는 학교명과 지역 등 구분 정보를 확인하고 한 학교를 선택한다.

### 2단계: 날짜 범위 선택

1. 사용자가 시작일과 종료일을 선택한다.
2. 시스템은 두 날짜가 모두 입력되었고 시작일이 종료일보다 늦지 않은지 검증한다.
3. 유효한 범위일 때 급식 조회를 진행할 수 있다.

### 3단계: 날짜별 중식 결과 표시

1. 시스템이 선택된 학교와 날짜 범위를 기준으로 중식 정보를 조회한다.
2. 결과를 급식일 오름차순으로 표시한다.
3. 각 결과에서 날짜, 메뉴, 열량, 영양정보 및 원산지 등 제공 가능한 정보를
   확인할 수 있다.

학교를 변경하면 기존 학교의 급식 결과는 더 이상 유효하지 않으므로 초기화한다.
날짜 범위를 변경한 뒤에는 변경된 조건으로 다시 조회해야 한다.

## 7. 기능 요구사항

### FR-1. 학교 검색

- 사용자는 학교명 전체가 아닌 일부만 입력해 검색할 수 있어야 한다.
- 공백만 있는 입력은 검색 요청으로 처리하지 않아야 한다.
- 검색 중임을 사용자에게 표시해야 한다.
- 검색 결과는 최대 20개까지 표시해야 한다.
- 일치하는 학교가 20개를 초과하면 결과가 일부만 표시되었음을 알리고 더 구체적인
  학교명을 입력하도록 안내해야 한다.
- 결과에는 최소한 학교명, 지역 또는 교육청명, 학교 종류를 표시해 동명 학교를
  구분할 수 있어야 한다.
- 학교 선택에 필요한 시도교육청 코드와 행정표준 코드는 시스템 내부에서
  유지하되 사용자에게 입력을 요구하지 않아야 한다.

### FR-2. 학교 선택

- 검색 결과에서 한 학교만 선택할 수 있어야 한다.
- 선택된 학교를 명확히 표시해야 한다.
- 학교를 선택하기 전에는 급식 조회를 실행할 수 없어야 한다.

### FR-3. 날짜 범위 선택

- 시작일과 종료일을 모두 선택할 수 있어야 한다.
- 시작일과 종료일은 조회 범위에 포함되어야 한다.
- 시작일이 종료일보다 늦은 경우 요청을 보내지 않고 수정 방법을 안내해야 한다.
- 조회 기간은 시작일과 종료일을 포함해 최대 31일이어야 한다.
- 31일을 초과하면 요청을 보내지 않고 허용 범위를 안내해야 한다.
- 유효한 학교와 날짜 범위가 준비된 경우에만 조회 동작을 활성화해야 한다.

### FR-4. 중식 조회

- 선택된 학교의 시도교육청 코드와 행정표준 코드, 시작일, 종료일을 사용해야 한다.
- 급식 구분은 중식으로 고정해야 한다.
- 중복 조회를 방지하고 조회 중 상태를 표시해야 한다.
- 성공한 결과는 날짜 오름차순으로 정렬해야 한다.

### FR-5. 급식 결과 표시

- 각 날짜에 대해 날짜와 요리명을 필수 정보로 표시해야 한다.
- NEIS가 제공한 경우 열량, 영양정보, 원산지, 급식인원수를 표시해야 한다.
- NEIS 응답의 줄바꿈과 알레르기 표기 등 메뉴 정보를 읽기 쉬운 형태로
  표현하되 원본 의미를 변경하지 않아야 한다.
- 일부 선택 정보가 없더라도 해당 날짜의 전체 결과를 실패로 처리하지 않고,
  제공되지 않은 항목임을 구분해야 한다.

### FR-6. MCP 도구 제공

- MCP 클라이언트가 Streamable HTTP 방식으로 서버에 연결해 제공 도구 목록을 조회할 수
  있어야 한다.
- 학교 이름 일부를 입력하면 후보 학교의 이름, 교육청명, 지역, 학교 종류,
  시도교육청 코드와 학교 코드를 반환하는 도구를 제공해야 한다.
- 선택한 학교의 시도교육청 코드와 학교 코드, 시작일, 종료일을 입력하면 중식 기준
  날짜별 급식 정보를 반환하는 도구를 제공해야 한다.
- MCP 도구도 공백 검색어, 잘못된 식별 코드, 역전된 날짜 범위와 31일 초과 범위를
  서버에서 검증해야 한다.
- 학교 검색 결과 없음과 급식 정보 없음은 오류와 구분되는 명확한 빈 상태로 반환해야
  한다.
- NEIS API 오류, 응답 지연 또는 연결 실패는 API 키, 내부 예외, 원본 요청 URL 등
  민감 정보를 포함하지 않는 명확한 MCP 도구 오류로 처리해야 한다.

## 8. 예외 및 상태 요구사항

| 상황 | 기대 동작 |
| --- | --- |
| 학교 검색어가 비어 있음 | 검색 요청을 보내지 않고 학교명을 입력하도록 안내한다. |
| 검색 결과가 없음 | 오류와 구분되는 빈 결과 메시지를 표시하고 다른 검색어를 제안한다. |
| 학교 검색 실패 | 서버 또는 외부 서비스 오류임을 표시하고 재시도할 수 있게 한다. |
| 학교가 선택되지 않음 | 급식 조회를 비활성화하고 학교 선택을 안내한다. |
| 날짜가 누락됨 | 누락된 날짜 입력을 안내하고 조회 요청을 보내지 않는다. |
| 시작일이 종료일보다 늦음 | 잘못된 범위임을 해당 입력 근처에 표시하고 요청을 보내지 않는다. |
| 날짜 범위가 31일을 초과함 | 최대 31일까지 조회할 수 있음을 표시하고 요청을 보내지 않는다. |
| 학교 검색 결과가 20개를 초과함 | 처음 20개를 표시하고 더 구체적인 검색어 입력을 안내한다. |
| 급식 정보가 없음 | 선택 기간에 등록된 중식이 없음을 정상적인 빈 상태로 표시한다. |
| 급식 조회 실패 | 기존 결과를 성공처럼 표시하지 않고 오류와 재시도 동작을 제공한다. |
| 응답 일부 정보가 없음 | 제공된 정보는 표시하고 누락 항목은 제공되지 않음으로 구분한다. |
| MCP 도구 입력이 잘못됨 | MCP 도구 오류로 잘못된 입력과 수정 방법을 안내한다. |
| MCP 도구의 NEIS 호출 실패 | 민감 정보 없이 외부 서비스 오류 또는 지연 상태를 안내한다. |

새로운 검색이나 조회가 시작되면 이전 오류 메시지는 현재 요청 상태에 맞게
갱신되어야 한다. 이전 요청보다 늦게 도착한 오래된 응답이 최신 화면을 덮어쓰지
않아야 한다.

## 9. 비기능 요구사항

### 사용성 및 접근성

- 키보드만으로 검색, 학교 선택, 날짜 입력 및 조회가 가능해야 한다.
- 입력 필드에는 연결된 레이블이 있어야 하며 상태와 오류는 색상에만 의존하지
  않아야 한다.
- 로딩 상태와 동적으로 변경된 결과를 보조 기술이 인지할 수 있어야 한다.
- 작은 화면에서도 3단계 흐름과 결과를 읽고 조작할 수 있어야 한다.

### 보안 및 개인정보

- NEIS API 키를 프론트엔드 코드, 브라우저 요청, 화면 또는 오류 메시지에
  노출하지 않아야 한다.
- 사용자가 입력한 학교명은 외부 API 호출 전에 서버에서 검증해야 한다.
- 사용자 개인정보를 수집하거나 저장하지 않아야 한다.

### 신뢰성

- 검색 결과 없음과 급식 정보 없음은 시스템 오류와 구분해야 한다.
- 외부 서비스 장애가 정상적인 빈 결과로 보이지 않아야 한다.
- 모든 날짜와 급식 데이터는 NEIS가 제공한 값을 근거로 표시해야 하며 임의로
  추정하지 않아야 한다.

## 10. 완료 조건

다음 시나리오가 통합 및 E2E 수준에서 확인되면 제품 요구사항을 충족한 것으로 본다.

1. 학교명 일부로 검색해 여러 결과 중 원하는 학교를 선택할 수 있다.
2. 유효한 날짜 범위를 선택하면 해당 기간의 중식이 날짜별로 표시된다.
3. 검색 결과 없음, 검색 결과 초과, 급식 정보 없음, 잘못된 날짜 범위가 각각 다른
   상태로 표시된다.
4. 학교 또는 날짜 조건을 바꾸면 오래된 결과가 현재 조건의 결과로 오인되지 않는다.
5. 백엔드 또는 NEIS 오류가 사용자에게 명확히 표시되고 재시도가 가능하다.
6. 브라우저가 NEIS API를 직접 호출하거나 NEIS API 키를 전달받지 않는다.
7. MCP 클라이언트가 도구 목록을 조회하고 학교 검색 및 중식 조회 도구를 호출할 수 있다.
